# Use service names (postgres/kafka) when running via docker compose
# Use localhost when running the API outside of Docker
DATABASE_URL=postgresql+asyncpg://wmm:wmm@postgres:5432/wmm
SECRET_KEY=change-me-to-a-long-random-secret-key
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30
KAFKA_BOOTSTRAP_SERVERS=kafka:29092
KAFKA_TOPIC_IMU=imu.readings
